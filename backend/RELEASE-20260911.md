# Scale OS 1.0.3 — verificación de publicación

Estado: correcciones locales; publicación y OAuth real aún pendientes de verificar.

## Orden

1. Publicar `scale-core-api` con los cambios de esta entrega. No incluir el archivo local ajeno `migrations/20260908_dadoo_hub.sql` ni las modificaciones ajenas de `schema.sql`.
2. Confirmar el commit exacto y la salud de API/base de datos en Owncoding Hub. Un HTTP 200 de `/health` no demuestra qué commit corre.
3. Publicar `scale-os` con versión visible 1.0.3. El footer se genera desde el componente compartido durante el build, incluida la landing.
4. Confirmar ambos commits publicados. Abrir una invitación original, no una URL antigua con `error=`. Verificar rechazo de token vencido/revocado antes de Google.
5. Con cuentas de prueba autorizadas: un uso acepta solo una cuenta; reutilizable queda pendiente y sin datos privados hasta aprobar; suspendido/removido solicita revisión sin reactivación automática. Confirmar rechazo y protección del rol ya activo.
6. Revisar UI móvil, recarga, contadores, copia del enlace y usuario unido sin duplicados. No consumir enlaces reales para probar.

## Evidencia local

`node --experimental-vm-modules --test test-invitation-full-flow-regression.mjs`

`node --experimental-vm-modules test-access-recovery.mjs`

`node test-invite-links.mjs`

La primera prueba recorre handlers reales con PostgreSQL emulado localmente y Google simulado; no acredita OAuth real ni concurrencia PostgreSQL real. Ver resultados finales en la continuidad de Scale OS del Segundo Cerebro.

## Bloqueo y recuperación

El navegador denegó acceso a Owncoding Hub porque no pudo verificar la política administrativa. No se eludió el control. No afirmar despliegue terminado hasta verificarlo.

Ante errores de autenticación/guardado o salud, conservar base y migraciones y revertir únicamente el cambio de comportamiento defectuoso. No volver a un backend incompatible con la vista de identidad ampliada, no borrar historial ni reactivar accesos masivamente. Esta entrega no requiere una nueva migración.
